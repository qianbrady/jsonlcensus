# Changelog

## v0.1.0 — 2025-12-01

首次发布：JSONL 字段普查器（纯标准库，Python >= 3.10）。

- `profile`：全量字段画像——出现率 / 类型分布 / 空值率 / 最大嵌套深度 / 截断示例值，一行命令输出 Markdown 报表
- `drift`：按时间顺序分桶对比 schema 漂移（新增 / 消失 / 类型变化 / 出现率偏移），默认前后两半，`--buckets`、`--min-delta` 可调
- `--limit N`：只扫描前 N 行
- 流式逐行解析，内存恒定 O(字段数)，支持 GB 级文件；坏行计数不中断；空白行 / 非对象行分别计数
- 递归深度超过 50 的字段标记「过深」，不再下钻
- 示例值去重截断（每字段前 5 个，24 字符内）；输出完全确定
- stdio 强制 UTF-8（GBK 控制台不崩）；退出码 0 / 1 / 2
- CI：GitHub Actions，ubuntu + windows × Python 3.10 / 3.12